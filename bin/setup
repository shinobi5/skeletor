#!/bin/bash

# Set current dir
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Remove .git from root
rm -rf "$DIR/../.git"

# Install npm modules
npm install

# Build and start project
npm run start
